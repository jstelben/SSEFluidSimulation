Click to simulate dropping an object in an area.
X will reset the fluid to a still state.
1, 2, and 3 will toggle between iterations of my development.
1 is without SSE optimizations.
2 is with a failed attempt at optimizing the Diffuse function.
3 is with working optimizations.
Esc to exit program.

Relevant code is found in FluidGridClass.cpp